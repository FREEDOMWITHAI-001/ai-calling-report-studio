# Copy to .env and adjust.
# PostgreSQL (percent-encode special characters in the password):
DATABASE_URL=postgresql+psycopg://user:pass%40word@host:5432/dbname
DB_SCHEMA=ai_report

# Or leave DATABASE_URL unset to use a local SQLite file (backend/data/app.db).
